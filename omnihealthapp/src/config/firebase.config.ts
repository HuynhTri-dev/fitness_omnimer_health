import auth from '@react-native-firebase/auth';

// React Native CLI sẽ tự lấy config từ android/app/google-services.json
// Không cần điền apiKey, projectId trực tiếp
export const firebaseApp = {}; // placeholder, SDK tự khởi tạo
export { auth };
